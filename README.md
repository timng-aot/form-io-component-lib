
  # Component library for form.io (Copy)

  This is a code bundle for Component library for form.io (Copy). The original project is available at https://www.figma.com/design/qWwYF981qweTHT5N75NrJ6/Component-library-for-form.io--Copy-.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  